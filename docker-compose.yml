services:
  chatbot-api:
    build: .
    container_name: chatbot-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - chatbot-network
    restart: unless-stopped

networks:
  chatbot-network:
    external: true
